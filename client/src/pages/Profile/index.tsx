// import { getUsersFunc } from "src/services/supabase/user";

const Profile = () => {
  // getUsers().then(console.log);
  // getUsersFunc().then(console.log);

  return (
    <>
      <div className="flex w-full flex-col gap-4 sm:p-2">
        <div>sdadsa</div>
      </div>
    </>
  );
};

export default Profile;
